# pdfase
A suite of privacy-first, client-side PDF utilities executing strictly within the local browser memory. Zero-server structural modifications including document merging, splitting, and compression.
